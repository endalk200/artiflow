---
"artiflow": minor
---

Remove the `artiflow skill install` command and distribute the Artiflow Skill through the Skills CLI. Install it with `npx skills add https://github.com/endalk200/artiflow` instead.
